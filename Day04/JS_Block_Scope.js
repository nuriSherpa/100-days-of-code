// Block Scope:
{
    let blockScopedVar = "I am a block-scoped variable";
    console.log(blockScopedVar); // Accessible here
}
// console.log(blockScopedVar); // Unaccessible here, will throw an error
// --- IGNORE ---